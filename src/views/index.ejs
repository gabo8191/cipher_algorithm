<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sistema de Cifrado por Bloques</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gray-50 font-sans">
    <div class="container mx-auto px-4 py-8 max-w-4xl">
      <div class="text-center mb-12">
        <h1 class="text-4xl font-bold text-gray-800 mb-3">Sistema de Cifrado por Bloques</h1>
        <div class="border-b border-gray-200 w-24 mx-auto mb-6"></div>
      </div>

      <div class="bg-white rounded-xl shadow-lg overflow-hidden mb-8">
        <div class="flex border-b border-gray-200">
          <button
            class="tab-btn px-8 py-4 font-medium text-gray-600 hover:text-blue-600 transition-colors border-b-2 border-transparent hover:border-blue-100 bg-white flex-1 flex items-center justify-center gap-2"
            onclick="openTab('encrypt')"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"
              />
            </svg>
            Cifrar
          </button>
          <button
            class="tab-btn px-8 py-4 font-medium text-gray-600 hover:text-blue-600 transition-colors border-b-2 border-transparent hover:border-blue-100 bg-white flex-1 flex items-center justify-center gap-2"
            onclick="openTab('decrypt')"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z"
              />
            </svg>
            Descifrar
          </button>
        </div>

        <div class="p-8">
          <div id="encrypt" class="tab-content space-y-6">
            <form action="/cifrar" method="POST">
              <div class="space-y-8">
                <div>
                  <label class="block text-sm font-semibold text-gray-500 uppercase mb-3"
                    >Clave Secreta</label
                  >
                    <input
                    type="text"
                    name="secret_key"
                    placeholder="Opcional - Ingrese su clave secreta"
                    maxlength="20"
                    pattern="^[A-Za-z0-9]*$"
                    title="No se permiten espacios ni la letra ñ. Máximo 20 caracteres."
                    class="w-full p-4 bg-gray-50 border-2 border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 font-mono text-gray-700 placeholder-gray-400 transition-all"
                    />
                </div>
                <div>
                  <label class="block text-sm font-semibold text-gray-500 uppercase mb-3"
                    >Texto a cifrar</label
                  >
                    <input
                    id="texto_claro"
                    name="texto_claro"
                    type="text"
                    required
                    placeholder="Ingrese el texto que desea cifrar (máximo 20 caracteres, sin ñ ni espacios)"
                    maxlength="20"
                    pattern="^[A-Za-z0-9]*$"
                    title="No se permiten espacios ni la letra ñ. Máximo 20 caracteres."
                    class="w-full p-4 bg-gray-50 border-2 border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 font-mono text-gray-700 placeholder-gray-400 transition-all"
                    >
                </div>

                <button
                  type="submit"
                  class="w-full bg-blue-600 text-white py-3.5 px-8 rounded-xl hover:bg-blue-700 transition duration-200 shadow-lg hover:shadow-xl font-medium"
                >
                  Cifrar Mensaje
                </button>
              </div>
            </form>
          </div>

          <div id="decrypt" class="tab-content hidden space-y-6">
            <form action="/descifrar" method="POST">
              <div class="space-y-8">
                <div>
                  <label class="block text-sm font-semibold text-gray-500 uppercase mb-3"
                    >Clave de descifrado</label
                  >
                  <input
                    type="text"
                    id="clave_descifrado"
                    name="clave_descifrado"
                    placeholder="Ingrese la clave de descifrado"
                    maxlength="20"
                    pattern="^[A-Za-z0-9]*$"
                    title="No se permiten espacios ni la letra ñ. Máximo 20 caracteres."

                    class="w-full p-4 bg-gray-50 border-2 border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 font-mono text-gray-700 placeholder-gray-400 transition-all"
                  />
                </div>

                <div>
                  <label class="block text-sm font-semibold text-gray-500 uppercase mb-3"
                    >Texto cifrado</label
                  >
                  <input
                    id="texto_cifrado"
                    name="texto_cifrado"
                    required
                    placeholder="Ingrese el texto cifrado que desea descifrar"
                    maxlength="20"
                    pattern="^[A-Za-z0-9]*$"
                    title="No se permiten espacios ni la letra ñ. Máximo 20 caracteres."
                    class="w-full p-4 bg-gray-50 border-2 border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 font-mono text-gray-700 placeholder-gray-400 transition-all"
                  >
                </div>
                <button
                  type="submit"
                  class="w-full bg-blue-600 text-white py-3.5 px-8 rounded-xl hover:bg-blue-700 transition duration-200 shadow-lg hover:shadow-xl font-medium"
                >
                  Descifrar Mensaje
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>

      </div>
    </div>


    <script>
      function openTab(tabName) {
        const tabContents = document.querySelectorAll('.tab-content');
        const tabButtons = document.querySelectorAll('.tab-btn');

        tabContents.forEach((content) => content.classList.add('hidden'));
        tabButtons.forEach((button) => button.classList.remove('active'));

        document.getElementById(tabName).classList.remove('hidden');
        event.currentTarget.classList.add('active');
      }

      document.addEventListener('DOMContentLoaded', () => {
        document.querySelector('.tab-btn').click();
      });
    </script>
  </body>
</html>
